::this will only show the output
@echo off
:: @ echo on => this is the default setting and will show all things happening in background
:: by default the name of Batch file is somthing like => "C:\Windows\system32\cmd.exe" but we can change it using "title" command
title my-batch-script-file
echo Hello World I Changed the "title" to "my-batch-script-file"
pause